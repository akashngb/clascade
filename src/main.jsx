import React from 'react'
import ReactDOM from 'react-dom/client'
import LimitDrop from './LimitDrop.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LimitDrop />
  </React.StrictMode>,
)
